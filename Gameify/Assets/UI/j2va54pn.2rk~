using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class AddDeck : MonoBehaviour
{
    public Transform deck;
    public void OnClick()
    {
        // cards = deck.GetComponentsInChildren<Transform>();
        Debug.Log(deck.GetComponentsInChildren<Transform>());
        /*
        GameObject newcard = Instantiate(card, new Vector3(0, 0, 0), Quaternion.identity);
        Card cardcomponent = newcard.GetComponent<Card>();
        cardcomponent.front.text = frontfield.text;
        cardcomponent.back.text = backfield.text;
        newcard.transform.SetParent(cards.transform);
        */
    }
}
